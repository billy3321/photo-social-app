h1 修改圖片

= simple_form_for(@photo, html: { :class: 'form-horizontal center', :multipart: true }) do |f|
  = render :partial => 'form', :locals => { :f => f }

= link_to '顯示圖片', @photo, class: "btn"
= link_to 'Back', photos_path, class: "btn"
